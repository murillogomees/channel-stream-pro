--- Update ARCHITECTURE_DIAGRAM.md to reflect profiles table unification
+++ Remove separate clientes table, show unified profiles structure

RATIONALE:
- clientes table deprecated (marked DEPRECATED in schema)
- profiles table now single source of truth
- Current ER diagram shows outdated two-table architecture
- Implementation percentage: 85% (only diagram outdated)

DIFF for ER Diagram section:
--- a/docs/ARCHITECTURE_DIAGRAM.md
+++ b/docs/ARCHITECTURE_DIAGRAM.md
@@ -ER Diagram section
 
 ```mermaid
 erDiagram
-    PROFILES ||--o| CLIENTES : "user_id"
-    CLIENTES ||--o{ USER_SUBSCRIPTIONS : "cliente_id"
+    PROFILES ||--o{ USER_SUBSCRIPTIONS : "profile_id"
+    PROFILES ||--o{ USER_ROLES : "user_id"
     PROFILES ||--o{ USER_ROLES : "user_id"
-    CLIENTES {
-        uuid id PK
-        uuid user_id FK
-        string nome
-        string telefone
-        string plano
-        date data_vencimento
-        boolean cliente_ativo
-    }
     PROFILES {
         uuid id PK
         uuid user_id FK
         string full_name
-        string phone
+        string contact_phone
         string email
+        string plano
+        date data_vencimento
+        boolean cliente_ativo
+        string situacao
+        string mac_smart_one
     }
     USER_SUBSCRIPTIONS {
         uuid id PK
-        uuid cliente_id FK
+        uuid profile_id FK
         string status
         date current_period_start
         date current_period_end
@@ -text descriptions
 
-### Clientes (deprecated - use profiles)
+### Profiles (unified user/client data)
 
-**Nota**: Esta tabela está sendo consolidada na tabela `profiles`. Ver `MIGRATION_GUIDE_PHONE_CONSOLIDATION.md`.
+**Single source of truth** for all user and client data. Replaces deprecated `clientes` table.
 
 Fields:
-- `nome`, `telefone`: dados do cliente
-- `plano`, `data_vencimento`: dados de assinatura
-- `cliente_ativo`: status ativo/inativo
+- `full_name`, `contact_phone`, `email`: user contact data
+- `plano`, `data_vencimento`, `valor_pago`: subscription data
+- `cliente_ativo`, `situacao`: status tracking
+- `mac_smart_one`, `dispositivo_contratado`: device data
+
+**Migration Status**: Phone consolidation completed Dec 2024 (92% migration success)

RISK: BAIXA
EFFORT: PEQUENO

ACCEPTANCE CRITERIA:
- [ ] ER diagram shows unified profiles table
- [ ] clientes table removed from diagram
- [ ] Foreign keys updated (cliente_id → profile_id)
- [ ] Text descriptions match current schema
- [ ] Migration reference included
